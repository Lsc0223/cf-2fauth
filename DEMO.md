# 演示指南

欢迎使用 2FA 安全管理系统！本文档将指导你快速体验所有功能。

## 🎬 快速演示

### 1️⃣ 登录系统

访问你的 Workers URL 后，你会看到登录页面：

- **用户名**: 输入任意用户名（例如：`demo`）
- **密码**: 输入任意密码（例如：`123456`）

点击"登录"后，系统会自动创建账户并登录。

> 💡 **提示**: 这是演示模式，适合个人使用。生产环境建议配置 OAuth 登录。

### 2️⃣ 添加第一个 2FA 账户

点击右上角的 **"➕ 添加账户"** 按钮：

**示例账户信息：**

- **账户名称**: `测试账户`
- **发行者**: `test.com`
- **密钥**: `JBSWY3DPEHPK3PXP`（这是一个测试密钥）
- **分类**: `测试`
- **验证码位数**: `6 位`
- **时间周期**: `30 秒`

点击"添加"后，你会看到账户卡片出现，验证码开始自动更新。

### 3️⃣ 使用验证码

- 验证码每 30 秒自动刷新
- 点击验证码可以复制到剪贴板
- 进度条显示剩余有效时间

**测试验证码**：
使用密钥 `JBSWY3DPEHPK3PXP` 生成的验证码应该与其他 2FA 应用相同。

### 4️⃣ 编辑账户

点击账户卡片上的 **✏️ 编辑** 按钮：

- 修改账户名称
- 更改分类
- 调整验证码设置

点击"保存"应用更改。

### 5️⃣ 搜索账户

在搜索框中输入关键词：

- 可以搜索账户名称
- 可以搜索发行者
- 实时过滤显示

### 6️⃣ 配置云端备份

点击 **"☁️ 备份"** 按钮，选择 **"WebDAV 配置"** 标签：

**免费 WebDAV 服务（推荐）：**

#### TeraCloud（10GB 免费）

1. 注册账号：https://teracloud.jp/
2. 配置信息：
   - **配置名称**: `TeraCloud`
   - **WebDAV URL**: `https://webdav.teracloud.jp/dav/`
   - **用户名**: 你的 TeraCloud 邮箱
   - **密码**: 你的 TeraCloud 密码

#### Nextcloud（自托管）

如果你有 Nextcloud：
- **WebDAV URL**: `https://你的域名/remote.php/dav/files/用户名/`
- 使用"应用专用密码"而不是主密码

### 7️⃣ 创建备份

配置 WebDAV 后：

1. 切换到 **"创建备份"** 标签
2. 选择 WebDAV 配置
3. 输入备份密码（例如：`backup123`）
4. 点击"创建备份"

备份文件会自动上传到云端，路径格式：
```
/2fa-backup/2024/01/15/backup-1705334400000.encrypted
```

### 8️⃣ 导出数据

点击 **"📤 导出"** 按钮：

**选项 1 - 加密导出：**
- 输入密码（例如：`export123`）
- 下载 `.encrypted` 文件

**选项 2 - 明文导出：**
- 留空密码
- 下载 `.json` 文件

> ⚠️ **警告**: 明文导出的文件包含未加密的密钥，请妥善保管！

### 9️⃣ 导入数据

点击 **"📥 导入"** 按钮：

**测试导入**：

创建一个测试文件 `test-import.json`：
```json
{
  "version": "1.0",
  "accounts": [
    {
      "name": "Google",
      "secret": "JBSWY3DPEHPK3PXP",
      "issuer": "google.com",
      "category": "邮箱"
    },
    {
      "name": "GitHub",
      "secret": "JBSWY3DPEHPK3PXQ",
      "issuer": "github.com",
      "category": "开发工具"
    }
  ]
}
```

导入步骤：
1. 选择文件
2. 如果是加密文件，输入密码
3. 勾选"合并导入"（如果不想覆盖现有数据）
4. 点击"导入"

### 🔟 删除账户

点击账户卡片上的 **🗑️ 删除** 按钮：

- 会弹出确认对话框
- 确认后账户会被永久删除
- 建议删除前先导出备份

## 📱 实际使用场景

### 场景 1：添加 GitHub 2FA

1. 访问 GitHub Settings -> Security -> Two-factor authentication
2. 点击"Set up using an app"
3. 选择"enter this text code"
4. 复制显示的密钥（例如：`a1b2 c3d4 e5f6 g7h8`）
5. 去掉空格：`a1b2c3d4e5f6g7h8`
6. 在本系统中添加账户，粘贴密钥
7. 使用生成的验证码完成 GitHub 设置

### 场景 2：添加 Google 账户 2FA

1. 访问 Google Account -> Security -> 2-Step Verification
2. 选择"Authenticator app"
3. 点击"Can't scan it?"
4. 复制显示的密钥
5. 在本系统中添加账户
6. 使用生成的验证码完成 Google 设置

### 场景 3：从 2FAS 迁移

1. 在 2FAS 应用中导出数据
2. 选择导出为 JSON 格式
3. 在本系统中导入该 JSON 文件
4. 系统会自动识别 2FAS 格式并转换

### 场景 4：多设备同步

**设备 A（主设备）：**
1. 配置 WebDAV
2. 添加所有账户
3. 创建云端备份

**设备 B（新设备）：**
1. 登录相同的 Workers URL
2. 配置相同的 WebDAV
3. 恢复最新备份

## 🎯 最佳实践

### 安全建议

1. **使用强密码**
   - 备份密码至少 12 位
   - 包含大小写字母、数字和符号

2. **定期备份**
   - 每周至少备份一次
   - 重要变更后立即备份

3. **保护备份**
   - 备份密码单独存储
   - 使用密码管理器

4. **分类管理**
   - 按用途分类（工作、个人、财务等）
   - 便于查找和管理

### 命名建议

- ✅ 好的命名：`GitHub (Work)`, `Google (Personal)`
- ❌ 不好的命名：`Account 1`, `Test`, `Untitled`

### 分类建议

- 📧 邮箱
- 💼 工作
- 🏦 财务
- 🎮 娱乐
- 🛒 购物
- 💻 开发工具
- 🔧 系统

## 🧪 测试功能

### 测试验证码生成

使用标准测试密钥验证 TOTP 实现是否正确：

**测试密钥**: `GEZDGNBVGY3TQOJQGEZDGNBVGY3TQOJQ`

**预期结果**（时间戳 1111111111）:
- 验证码应该是: `050471`

你可以在其他 2FA 应用中使用相同密钥验证结果是否一致。

### 测试加密导出

1. 添加一个测试账户
2. 导出并设置密码
3. 删除账户
4. 导入刚才导出的文件
5. 验证账户是否恢复

### 测试 WebDAV

1. 配置 WebDAV
2. 创建测试备份
3. 访问 WebDAV 服务确认文件存在
4. 删除本地账户
5. 从云端恢复
6. 验证数据完整性

## 💡 小技巧

### 快速添加

如果你有多个账户要添加，可以：
1. 准备一个文本文件
2. 每行格式：`名称,密钥,发行者`
3. 使用导入功能批量添加

### 快速查找

使用搜索框的快捷键：
- 直接开始输入即可搜索
- 按 `Esc` 清空搜索

### 快速复制

- 点击验证码即可复制
- 无需选择文本
- 支持触摸设备

## 🐛 遇到问题？

### 验证码不正确

1. 检查系统时间是否准确
2. 确认密钥格式正确（Base32）
3. 确认时间周期设置（通常是 30 秒）
4. 尝试在其他 2FA 应用中测试相同密钥

### 无法创建备份

1. 检查 WebDAV 配置是否正确
2. 确认网络连接
3. 检查 WebDAV 服务器状态
4. 查看浏览器控制台错误信息

### 导入失败

1. 检查文件格式
2. 如果是加密文件，确认密码正确
3. 确认 JSON 格式正确
4. 尝试用文本编辑器打开查看内容

## 📚 更多资源

- [完整文档](README.md)
- [API 文档](docs/API.md)
- [常见问题](docs/FAQ.md)
- [部署指南](DEPLOYMENT.md)

## 🎉 开始使用

准备好了吗？

1. 登录系统
2. 添加你的第一个 2FA 账户
3. 配置云端备份
4. 开始安全的双因素认证之旅！

---

**祝使用愉快！如有问题，请查看 FAQ 或提交 Issue。**
