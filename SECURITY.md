# 安全策略

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| 1.0.x | :white_check_mark: |

## 报告安全漏洞

如果你发现了安全漏洞，请**不要**公开提交 Issue。

请通过以下方式私下报告：
1. 发送邮件到项目维护者
2. 在 GitHub 上使用私密漏洞报告功能

我们会尽快响应并处理安全问题。

## 安全最佳实践

### 部署安全

1. **密钥管理**
   - 使用强随机密钥（至少 32 字节）
   - 定期轮换密钥
   - 永远不要在代码中硬编码密钥
   - 使用 `wrangler secret` 管理敏感信息

2. **访问控制**
   - 考虑使用 Cloudflare Access 限制访问
   - 配置 IP 白名单
   - 启用地理位置限制

3. **备份安全**
   - 使用强密码保护备份文件
   - 定期备份数据
   - 将备份存储在安全的位置

### 使用安全

1. **密码管理**
   - 使用强密码
   - 不要重复使用密码
   - 定期更改密码

2. **2FA 密钥**
   - 妥善保管 2FA 密钥
   - 不要分享密钥
   - 删除不使用的账户

3. **浏览器安全**
   - 使用最新版本的浏览器
   - 不要在公共设备上使用
   - 使用后及时退出

## 已知限制

1. **会话管理**
   - JWT Token 默认 2 小时过期
   - 无法主动撤销 Token（除非更换 JWT_SECRET）

2. **速率限制**
   - 基于 IP 地址
   - 可能被 VPN/代理绕过

3. **数据存储**
   - 依赖 Cloudflare KV
   - KV 有最终一致性

## 安全审计

本项目尚未经过专业安全审计。使用前请评估风险。

## 依赖安全

本项目无第三方运行时依赖，所有功能使用 Web 标准 API 实现。

开发依赖：
- wrangler: Cloudflare 官方工具

## 数据加密

- **传输加密**: HTTPS (由 Cloudflare 提供)
- **存储加密**: AES-GCM-256
- **密钥派生**: PBKDF2 (100,000 迭代)

## 合规性

本项目不收集个人身份信息（PII），所有数据存储在用户自己的 Cloudflare 账户中。

## 更新通知

安全更新将在以下渠道发布：
- GitHub Releases
- CHANGELOG.md
- Security Advisories

## 责任声明

本项目按"原样"提供，不提供任何明示或暗示的保证。使用本软件的风险由用户自行承担。
